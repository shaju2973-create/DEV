fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
pyotp==2.9.0
qrcode[pil]==8.0
httpx==0.28.1
websockets==14.2
redis==5.2.1
python-multipart==0.0.20
email-validator==2.2.0
cryptography==44.0.0
slowapi==0.1.9

# LiveMarketTicker primary provider (FYERS API v3 WebSocket).
# Pulls heavy transitive deps (boto3/aws-lambda-powertools); imported lazily so
# it only loads when MARKET_DATA_PROVIDER=fyers. See docs/LIVE_MARKET_TICKER.md.
fyers-apiv3==3.1.16

# LiveMarketTicker fallback provider (Upstox Market Data Feed V3, protobuf).
# Bundled so MARKET_DATA_PROVIDER=upstox works out of the box; imported lazily
# so it only loads when Upstox is selected/enabled.
upstox-python-sdk==2.15.0

